/**
 * Resolve BridgedAI credentials for HTTP clients.
 * Org API keys (brdg_*) must use x-api-key, not Authorization: Bearer.
 */
/** Must match backend ApiKeyService.BRIDGED_API_KEY_PATTERN */
export declare const BRIDGED_API_KEY_PATTERN: RegExp;
export declare const MAX_BRIDGED_CREDENTIAL_LENGTH = 256;
export declare const ORG_ID_PATTERN: RegExp;
export type BridgedAuthCredential = {
    apiKey?: string;
    accessToken?: string;
};
export declare function isBridgedApiKey(value: string): boolean;
export declare function isValidBridgedOrgId(orgId: string): boolean;
/** Split a single secret into api-key vs JWT Bearer. */
export declare function splitBridgedCredential(token: string): BridgedAuthCredential;
export type ResolveBridgedAuthInput = {
    apiKey?: string;
    accessToken?: string;
    bridgedaiToken?: string;
};
/**
 * Prefer explicit api-key; if access-token is a brdg_* key, route to x-api-key even when both are set.
 */
export declare function resolveBridgedAuth(input: ResolveBridgedAuthInput): BridgedAuthCredential;
export declare function requireBridgedAuth(input: ResolveBridgedAuthInput, message?: string): BridgedAuthCredential;
