"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseEmitDemoSignals = parseEmitDemoSignals;
exports.signalMetadataFromEmitDemo = signalMetadataFromEmitDemo;
/** Metadata for signal envelopes — production vs demo data source tagging. */
function parseEmitDemoSignals(raw, defaultValue = false) {
    if (typeof raw === 'boolean')
        return raw;
    if (raw === undefined || raw === '')
        return defaultValue;
    return String(raw).trim().toLowerCase() === 'true';
}
function signalMetadataFromEmitDemo(emitDemoSignals) {
    return parseEmitDemoSignals(emitDemoSignals)
        ? { dataSource: 'demo' }
        : { dataSource: 'production', emitDemo: false };
}
