export type LogFn = (message: string) => void;
export interface SafeLogger {
    info: LogFn;
    warning: LogFn;
    error: LogFn;
    debug: LogFn;
}
export declare function createSafeLogger(apiKey?: string, impl?: Partial<SafeLogger>): SafeLogger;
