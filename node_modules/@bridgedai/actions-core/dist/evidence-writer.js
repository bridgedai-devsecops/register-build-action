"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeJsonFile = writeJsonFile;
exports.readJsonFile = readJsonFile;
exports.writeAuthSessionContext = writeAuthSessionContext;
exports.writeSubjectMetadata = writeSubjectMetadata;
exports.listJsonFilesInDir = listJsonFilesInDir;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const signal_writer_1 = require("./signal-writer");
async function writeJsonFile(filePath, data) {
    await (0, signal_writer_1.ensureDirForFile)(filePath);
    const abs = path.resolve(filePath);
    const json = `${JSON.stringify(data, null, 2)}\n`;
    await fs.promises.writeFile(abs, json, { encoding: 'utf8', mode: 0o644 });
    return abs;
}
async function readJsonFile(filePath) {
    const abs = path.resolve(filePath);
    if (!fs.existsSync(abs))
        throw new Error(`File not found: ${abs}`);
    const raw = await fs.promises.readFile(abs, 'utf8');
    return JSON.parse(raw);
}
async function writeAuthSessionContext(filePath, ctx) {
    return writeJsonFile(filePath, ctx);
}
async function writeSubjectMetadata(filePath, meta) {
    return writeJsonFile(filePath, meta);
}
function listJsonFilesInDir(dirPath) {
    const abs = path.resolve(dirPath);
    if (!fs.existsSync(abs))
        return [];
    const out = [];
    const walk = (dir) => {
        for (const ent of fs.readdirSync(dir, { withFileTypes: true })) {
            const p = path.join(dir, ent.name);
            if (ent.isDirectory())
                walk(p);
            else if (ent.isFile() && (ent.name.endsWith('.json') || ent.name.endsWith('.jsonl')))
                out.push(p);
        }
    };
    walk(abs);
    return out.sort();
}
