"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isVerificationLevel = isVerificationLevel;
exports.assertVerificationLevel = assertVerificationLevel;
const VALID = new Set([
    'cryptographic_verified',
    'provider_authoritative',
    'runtime_observed',
    'scanner_observed',
    'inferred',
    'manual',
    'demo',
]);
function isVerificationLevel(v) {
    return VALID.has(v);
}
function assertVerificationLevel(v) {
    if (!isVerificationLevel(v)) {
        throw new Error(`Invalid verificationLevel: ${v}`);
    }
    return v;
}
