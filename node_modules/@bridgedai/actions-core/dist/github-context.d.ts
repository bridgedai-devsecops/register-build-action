import type { SignalSubject } from './signal-envelope';
export interface GitHubWorkflowContext {
    repository: string;
    repositoryId?: string;
    repositoryOwner?: string;
    branch?: string;
    ref?: string;
    refType?: string;
    commitSha: string;
    commitAuthor?: string;
    actor?: string;
    eventName?: string;
    workflowName?: string;
    workflowRef?: string;
    workflowRunId: string;
    workflowRunNumber?: string;
    runAttempt: string;
    jobName: string;
    runnerName?: string;
    runnerOs?: string;
    runnerArch?: string;
    /** Best-effort; GitHub Actions does not expose runner labels in env — use webhook or API for full labels. */
    runnerLabels?: string[];
    runnerGroup?: string;
    workflowJobId?: string;
    pullRequestNumber?: number;
    environment?: string;
    changedFiles?: string[];
}
export declare function extractGitHubContext(changedFiles?: string[]): GitHubWorkflowContext;
export declare function githubContextToSignalSubject(ctx: GitHubWorkflowContext, extra?: Partial<SignalSubject>): SignalSubject;
/** Safe subset of GitHub env vars for evidence files (no secrets). */
export declare function safeGithubEnvRecord(): Record<string, string>;
